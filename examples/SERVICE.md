<!--
  This file is partially generated by handover-kit.
  Do not remove the "handoverkit:" HTML comments below each heading — they
  store a hash of the files each section was derived from, which is how
  `handoverkit check` knows a section has gone stale.
  Everything a section generates is rewritten on every run. Text you write
  inside a "handoverkit:notes" block is yours and is carried over untouched.
-->

# Service Handover Doc

## Overview
<!-- handoverkit:id=overview hash=41c16b259959 sources=README.md,package.json -->
**handover-kit**

Keep service documentation alive across handovers: generate a living SERVICE.md and flag it when code changes but the docs don't.

<!-- handoverkit:notes:start id=overview -->
handover-kit is a CLI, not a long-running service — there is nothing to deploy
or page anyone about. This file is generated by the tool against its own repo,
so it doubles as the fixture we notice regressions in.
<!-- handoverkit:notes:end id=overview -->

## Environment & Config
<!-- handoverkit:id=environment hash=b24364742d39 sources=.env.example,.env.sample,.env.template -->
| Variable | Default | Notes |
| --- | --- | --- |
| `PORT` | `3000` | Port the HTTP server listens on |
| `DATABASE_URL` | `postgres://localhost:5432/handoverkit` | Postgres connection string, ask #infra-team if you don't have one |
| `FEATURE_FLAGS_API_KEY` | _(no default)_ | Feature flag provider API key |
| `REDIS_URL` | `redis://localhost:6379` | Redis connection for the job queue |

<!-- handoverkit:notes:start id=environment -->
None of these variables are read by the CLI. `.env.example` is sample input
kept so the generator has a realistic file to parse — don't go looking for the
Postgres or Redis this table implies.
<!-- handoverkit:notes:end id=environment -->

## Local Setup
<!-- handoverkit:id=local-setup hash=51a2f9d202b0 sources=package.json -->
| Command | Runs |
| --- | --- |
| `npm run build` | `tsc -p tsconfig.json` |
| `npm run typecheck` | `tsc --noEmit -p tsconfig.test.json` |
| `npm run test` | `node --import tsx --test tests/*.test.ts` |
| `npm run dev` | `tsx src/cli.ts` |
| `npm run generate` | `tsx src/cli.ts generate` |
| `npm run check` | `tsx src/cli.ts check` |
| `npm run prepublishOnly` | `npm run typecheck && npm test && npm run build` |

Runtime dependencies: `commander`.

<!-- handoverkit:notes:start id=local-setup -->
Tests run on `node:test` through the `tsx` loader, so there is no test-runner
dependency to keep current. `npm run check` is the same command CI runs — if it
passes locally it passes there.
<!-- handoverkit:notes:end id=local-setup -->

## Deployment
<!-- handoverkit:id=deployment hash=c8167fea8d30 sources=.github/workflows/handover-check.yml,.gitlab-ci.yml -->
Deployment is driven by more than one pipeline:

- **GitHub Actions** — `.github/workflows/handover-check.yml`
- **GitLab CI** — `.gitlab-ci.yml`

<!-- handoverkit:notes:start id=deployment -->
_No hand-written notes yet — anything you write between these two markers survives `handoverkit generate`._
<!-- handoverkit:notes:end id=deployment -->

## Known Issues
<!-- handoverkit:id=known-issues hash=a68809ab2e6a sources=src/cli.ts,src/marker.ts,src/core/check.ts,src/core/generate.ts,src/core/hash.ts,src/core/notes.ts,src/core/sections.ts,src/core/parsers/ci.ts,src/core/parsers/codeowners.ts,src/core/parsers/env.ts,src/core/parsers/fsUtil.ts,src/core/parsers/packageJson.ts,src/core/parsers/walk.ts,src/providers/GithubProvider.ts,src/providers/GitlabProvider.ts,src/providers/VcsProvider.ts,tests/hash.test.ts,tests/notes.test.ts,tests/parsers.test.ts,tests/roundtrip.test.ts -->
_No TODO/FIXME markers found in the 20 scanned source files._

<!-- handoverkit:notes:start id=known-issues -->
_No hand-written notes yet — anything you write between these two markers survives `handoverkit generate`._
<!-- handoverkit:notes:end id=known-issues -->

## Ownership
<!-- handoverkit:id=ownership hash=b37c9dde2f9d sources=CODEOWNERS,.github/CODEOWNERS,docs/CODEOWNERS -->
| Path | Owners |
| --- | --- |
| `src/providers/` | @rinaldy-pasya |
| `src/core/` | @rinaldy-pasya |

<!-- handoverkit:notes:start id=ownership -->
_No hand-written notes yet — anything you write between these two markers survives `handoverkit generate`._
<!-- handoverkit:notes:end id=ownership -->
