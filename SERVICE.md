<!--
  This file is partially generated by handover-kit.
  Do not remove the <!-- handoverkit:... --> comments below each heading —
  they store a hash of the files each section was derived from, which is
  how `handoverkit check` knows a section has gone stale.
  Freely edit the prose; the hash only tracks the *source files*, not
  your edits to this document.
-->

# Service Handover Doc

## Overview
<!-- handoverkit:id=overview hash=8e63521b9570 sources=README.md,package.json -->
**handover-kit**

Keep service documentation alive across handovers: generate a living SERVICE.md and flag it when code changes but the docs don't.

## Environment & Config
<!-- handoverkit:id=environment hash=b24364742d39 sources=.env.example,.env.sample,.env.template -->
| Variable | Default | Notes |
| --- | --- | --- |
| `PORT` | `3000` | Port the HTTP server listens on |
| `DATABASE_URL` | `postgres://localhost:5432/handoverkit` | Postgres connection string, ask #infra-team if you don't have one |
| `FEATURE_FLAGS_API_KEY` | _(no default)_ | Feature flag provider API key |
| `REDIS_URL` | `redis://localhost:6379` | Redis connection for the job queue |

## Local Setup
<!-- handoverkit:id=local-setup hash=d83bdbba7479 sources=package.json -->
| Command | Runs |
| --- | --- |
| `npm run build` | `tsc -p tsconfig.json` |
| `npm run dev` | `tsx src/cli.ts` |
| `npm run generate` | `tsx src/cli.ts generate` |
| `npm run check` | `tsx src/cli.ts check` |

## Deployment
<!-- handoverkit:id=deployment hash=a311c28a2de6 sources=.gitlab-ci.yml -->
Deployment is driven by **GitLab CI**. Pipeline definition: `.gitlab-ci.yml`.

## Known Issues
<!-- handoverkit:id=known-issues hash=8f486d40946f sources=src/cli.ts,src/core/check.ts,src/core/generate.ts,src/core/hash.ts,src/core/parsers/ci.ts,src/core/parsers/codeowners.ts,src/core/parsers/env.ts,src/core/parsers/fsUtil.ts,src/core/parsers/packageJson.ts,src/core/parsers/walk.ts,src/core/sections.ts,src/providers/GithubProvider.ts,src/providers/GitlabProvider.ts,src/providers/VcsProvider.ts -->
_No TODO/FIXME markers found in scanned source files._

## Ownership
<!-- handoverkit:id=ownership hash=fc76a323b332 sources=CODEOWNERS,.github/CODEOWNERS -->
| Path | Owners |
| --- | --- |
| `src/providers/` | @rinaldy-pasya |
| `src/core/` | @rinaldy-pasya |
